<IfModule mod_rewrite.c> 
    Options -MultiViews 
    RewriteEngine On 


    RewriteRule ^public/(.*)$ /$1 [L,NC,R]
 
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f 
    RewriteRule ^ index.php [L] 
      
      
 
#    RewriteEngine On 
#    RewriteCond %{SERVER_PORT} 80 
#    RewriteRule ^(.*)$ https://roster.qk.com.au/$1 [R,L] 
</IfModule>